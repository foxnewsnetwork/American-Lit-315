<!-- show page for ads -->
<h2><%= @company.name %></h2>
<h3><%= @ad.id %></h3>
<%= image_tag @ad.picture.url %>
<!-- Put other stuff in here -->
<%= link_to "XML-view", company_ad_path( @ad ) + ".xml"  %>

